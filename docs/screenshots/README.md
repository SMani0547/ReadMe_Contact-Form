# Screenshots

Add the main project screenshot here using this filename:

```text
contact-form-preview.png
```

Recommended size: 1600 × 900 pixels (16:9).

After adding the screenshot, replace the temporary `placehold.co` image in the root `README.md` with:

```html
<img width="100%" src="./docs/screenshots/contact-form-preview.png" alt="Contact form preview" />
```
